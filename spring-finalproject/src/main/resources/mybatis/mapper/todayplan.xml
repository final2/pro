<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.finalproject.TodayPlanDao">

	<insert id="addTodayPlan" parameterType="TP">
		insert into todaywork(no, todaywork_title, todaywork_contents, regdate, employee_no)
		values(my_seq.nextval, #{title}, #{contents}, sysdate, '90000006')
	</insert>
	
	<delete id="removeTodayPlan" parameterType="TP">
		delete from todaywork where employee_no = #{empNo} and no = #{no}
	</delete>

	<select id="getTodayPlanList" resultType="TP">
		select 
			no					as no,
			todaywork_title		as title,
			todaywork_contents	as contents,
			regdate				as regdate,
			employee_no			as empNo
		from todaywork
	</select>
	
	<select id="getTodayPlanListByEmpNo" parameterType="int">
		select 
			no					as no,
			todaywork_title		as title,
			todaywork_contents	as contents,
			regdate				as regdate,
			employee_no			as empNo
		from todaywork
		where employee_no = #{empNo}
			
	</select>
</mapper>    